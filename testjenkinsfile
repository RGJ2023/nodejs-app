pipeline{
    agent any

    stages{
        stage('Dep Install'){
            agent {docker {image 'node'}}
            steps{
                sh 'npm install'
                sh 'npm pack'
            }
        }
    }
}