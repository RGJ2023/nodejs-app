pipeline{
    agent any

    stages{
        stage('Dep Install'){
            agent {docker {image 'node:14'}}
            steps{
                sh 'npm install'
                sh 'npm pack'
            }
        }
    }
}