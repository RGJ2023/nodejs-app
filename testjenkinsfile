pipeline{
    agent any

    stages{
        stage('Dep Install'){
            agent {docker {image 'node:14'}}
            steps{
                sh 'sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
                sh 'npm pack'
            }
        }
    }
}